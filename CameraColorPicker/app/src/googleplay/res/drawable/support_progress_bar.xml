<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/support_bar_background"
                android:centerY="0.75"
                android:endColor="@color/support_transparent_black"
                android:startColor="@color/support_bar_background" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/support_bar_progress"
                    android:centerY="0.75"
                    android:endColor="@color/support_transparent_black"
                    android:startColor="@color/support_bar_progress" />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/support_bar_progress"
                    android:centerY="0.75"
                    android:endColor="@color/support_transparent_black"
                    android:startColor="@color/support_bar_progress" />
            </shape>
        </clip>
    </item>

</layer-list>